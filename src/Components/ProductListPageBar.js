import React from 'react';
import styled from '@emotion/styled';

const Container = styled.div`
  ${
    '' /* background-color: rgb(144, 151, 151);
  height: 100px;
  margin-bottom: 20px;
  width: 85%;
  display: flex;
  justify-content: center;

  input {
    width: 210px;
    height: 40px;
    margin: 20px;
    padding-left: 15px;
    box-sizing: border-box;
  } */
  }
`;

const ProductListPageBar = (props) => {
  return (
    <>
      <Container></Container>
    </>
  );
};

export default ProductListPageBar;
